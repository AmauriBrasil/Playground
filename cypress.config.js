module.exports = {
  e2e: {
    setupNodeEvents(on, config) {
      // implement node event listeners here
    },
    // URL do projeto a ser executado
    baseUrl: 'https://playground.cyskills.com.br',

    // Resolução da tela do projeto
    viewportWidth: 1920,
    viewportHeight: 1080,
    video: true,
  },
};
